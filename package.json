{
  "name": "facecheck-api",
  "version": "1.0.0",
  "description": "Middleware API for FaceCheck.ID integration with Bubble",
  "main": "api/facecheck.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "engines": {
    "node": "18.x"
  }
}
